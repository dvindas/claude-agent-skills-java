# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This project is in its initial setup phase — no source code or build configuration exists yet. The IntelliJ IDEA project is configured for **Java 25** with output directory `out/`.

## Environment

- **JDK**: Java 25
- **IDE**: IntelliJ IDEA
- **Build system**: Not yet configured (choose Maven or Gradle when adding source code)

## Getting Started

When adding source code, standard Java project conventions apply:
- Maven: add `pom.xml` at root, sources under `src/main/java/`, tests under `src/test/java/`
- Gradle: add `build.gradle` (or `build.gradle.kts`) at root, same source layout

Once a build system is in place, update this file with the actual build, test, and run commands.

